name: test

on:
  pull_request:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.6', '3.7']
    steps:
      - uses: actions/checkout@v1
      - uses: ./.github/actions/test
        env:
          PYTHON_VERSION: ${{ matrix.python }}
